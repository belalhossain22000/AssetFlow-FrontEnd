export type SaleItem = {
    productId: string;
    quantity: number;
    buyerName: string;
    saleDate: string; 
  };

  export type TloginUser={
    username:string;
    password:string
  }
  export type TRegisterUser={
    username:string;
    email:string;
    password:string
  }